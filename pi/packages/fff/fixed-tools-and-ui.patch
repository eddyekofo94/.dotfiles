--- a/src/index.ts
+++ b/src/index.ts
@@ -550,6 +550,10 @@
         }
       }
 
+      // The isolated Eddy pilot intentionally fixes FFF to tools-and-ui.
+      // Ignore any mode entry persisted by a prior interactive session.
+      currentMode = "tools-and-ui";
+
       registerAutocompleteProvider(ctx);
       await ensureFinder(activeCwd);
     } catch (e: unknown) {
@@ -1061,6 +1065,11 @@
         return;
       }
 
+      if (arg !== "tools-and-ui") {
+        ctx.ui.notify("FFF mode is fixed to 'tools-and-ui' in this isolated pilot", "warning");
+        return;
+      }
+
       const newMode = arg as FffMode;
       const oldMode = getMode();
       setMode(newMode);

# Active Goal

Fixture loop records for `pi/tests/session_validation.py`. Nothing here
describes real work; the file exists so compaction continuity is asserted
against a fixed tree instead of this repository's mutable planning prose.

`pi-compaction-fixture-closed` closed earlier and deliberately has no
`decisions.md`. It is listed first on purpose: first-backticked-slug-wins
selection would stop here, load nothing, and drop every obligation below.

`pi-compaction-fixture-active` is the open goal and owns
`.working/interviews/pi-compaction-fixture-active/decisions.md`. Slug selection
must skip the closed goal and reach this record.

Stop condition: the settled decisions survive manual, repeated, and threshold
compaction.

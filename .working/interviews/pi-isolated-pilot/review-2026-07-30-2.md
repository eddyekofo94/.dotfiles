# Pi interaction refinement review — 2026-07-30, pass 2

## Scope

Fresh two-axis review of the force-redraw, cursor presentation, and
slash-ranking refinement selected by
`physical-qa-2026-07-30-2.md`.

## First review

### Standards

Five findings:

- painted-cursor stripping did not cover the reported `ESC[27m` terminator;
- optional usage persistence could block the real submit path;
- usage persistence could follow a symlink and did not enforce private mode;
- the decisions record still described the superseded review as current;
- DECSCUSR byte values lacked exact regression assertions.

### Fidelity

Two findings:

- painted-cursor stripping and its unit test covered only `ESC[0m`;
- most-recent ranking was calculated across unrelated commands instead of the
  currently matching suggestions.

## Fixes

- Cursor stripping accepts both reverse-video reset forms and tests both.
- Matching suggestions calculate their own most-recent command.
- Usage persistence fails open, rejects symlinks and multiply linked files,
  locks the file to `0600`, and has a real hostile-path Herdr regression.
- Exact blinking block/bar DECSCUSR bytes are asserted.
- Workflow records distinguish the superseded physical pass from this
  refinement.

## Post-fix review

- Standards findings: **0**
- Fidelity findings: **0**

Physical Ghostty acceptance remains open; this review does not claim cursor
blink appearance, window-unfocus outline behavior, or tactile key delivery.

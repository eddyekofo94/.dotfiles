# This is Git's per-user configuration file.
[user]
	email = eddy.ekofo@external.equans.com
	name = Eddy Ekofo
[init]
    defaultBranch = master
[http]
    sslVerify = false

[credential "https://github.com"]
	helper =
	helper = !/usr/bin/gh auth git-credential

[pager]
    diff = delta
    log = delta
    reflog = delta
    show = delta
[delta]
    features = side-by-side line-numbers decorations
    syntax-theme = Catppuccin-mocha
    navigate = true
    dark = true

[core]
    filemode = false
[interactive]
    diffFilter = delta --color-only

[delta "decorations"]
    commit-decoration-style = bold yellow ul
    file-style = bold blue ul
    file-decoration-style = none
    hunk-header-decoration-style = omit
    hunk-header-file-style = magenta
    line-numbers = true
    line-numbers-right-style = "#5c6370"
    line-numbers-left-style = "#5c6370"
    line-numbers-minus-style = "#9A353D"
    line-numbers-plus-style = "#109868"
    minus-emph-style = bold "#9A353D" 52
    minus-empty-line-marker-style = normal "#9A353D"
    minus-non-emph-style = dim red
    minus-style = bold red
    plus-emph-style = bold green 22
    plus-empty-line-marker-style = normal "#002800"
    plus-non-emph-style = dim green
    plus-style = bold syntax
    whitespace-error-style = reverse red
    zero-style = dim syntax

[diff]
    ignoreSubmodules = dirty
